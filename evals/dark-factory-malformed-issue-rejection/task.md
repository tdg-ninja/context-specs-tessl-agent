You are handling a GitHub issue that has the `dark-factory` label, but the body is malformed: it has a goal sentence and no `## Scope`, `## Acceptance criteria`, or `## Constraints` sections.

State what the Dark Factory issue implementation agent should do before any implementation work. Include the report behavior and whether repo files should be edited.
