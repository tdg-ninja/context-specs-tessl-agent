A Context Specs PR is red. The failed checks are: `Local deterministic checks / test-cli` failed with an assertion from `scripts/test-cli.sh`; `Tessl skill assurance / quality review` failed because `skills/pr-check-triage` has stale or missing catalog review metadata; `Tessl change risk` completed and says human review is required by policy; and `Dark Factory issue dispatch` was cancelled after a GitHub-hosted runner startup timeout. Describe how the `pr-check-triage` skill should summarize the failures and recommend next actions.
