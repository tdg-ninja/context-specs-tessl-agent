An automated reviewer left four findings on a Context Specs feature PR: one clear typo in changed docs, one ambiguous architectural suggestion, one request for a feature outside the PRD, and one complex refactor that would change behavior. Describe how the `address-feedback` skill should triage and respond, including what it should fix, what it should comment on, and what it must avoid.
